<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Vomit Profusely{{else}}Eat it{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Throw it in the trash!</button>
</li>